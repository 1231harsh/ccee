name: Keep Render App Awake

on:
  schedule:
    # every 10 minutes
    - cron: "*/10 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send Request
        run: curl -I https://cceeportal.onrender.com